syntax = "proto3";

option go_package = "github.com/zyfy29/gyt-proto/latency;latency";

service LegacyService {
  rpc GetNextLegacy (LegacyRequest) returns (LegacyResponse) {}
}

message LegacyRequest {
  // リクエストは空です
}

message LegacyResponse {
  double value = 1;
}
